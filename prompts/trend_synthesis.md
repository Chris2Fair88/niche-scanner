You are analyzing a short list of YouTube videos that are breaking out
*right now* -- statistically unusual view velocity relative to their age --
within a small set of niches a creator is actively considering content for.
This is not the full periodic viability scan; it's a narrower, more
frequent check for what's currently working, so upcoming video ideas can be
adapted to ride a real signal instead of guessing.

You will receive, per niche: a short list of candidate videos with title,
channel, view count, hours since publish, computed views/hour velocity, and
a robust outlier score (how far above that niche's own recent median
velocity each video sits -- "n/a (sample too small for robust stats)" means
too few recent videos existed to compute one, so the video only cleared a
stricter absolute-velocity floor instead). All numbers are already
computed -- do not recompute, re-rank, or restate them as a table; that
table is generated separately and appended by the calling program.

Write ONLY the following two sections, in this order, as markdown with
these exact headers:

## What's Working Right Now
For each niche with at least one candidate, 2-3 sentences: what pattern do
the outlier videos share (topic angle, title/hook style, format, timing)
that plausibly explains why they're outperforming their niche's recent
baseline right now? Interpret the pattern -- don't just describe the
numbers, which are already in the table. Be explicit about confidence: a
single outlier video is a data point, not a trend; only call something a
"pattern" when multiple candidates in the niche converge on the same angle.
If a niche has zero candidates this run, say so in one sentence and don't
speculate about why.

## Concrete Adaptation Suggestions
For each niche with at least one candidate, 2-4 specific, actionable video
ideas that adapt the observed pattern -- not generic advice like "post more
consistently," "improve your thumbnails," or "study what's working." Each
suggestion should name a concrete angle, hook, title direction, or format a
creator could film this week, tied directly to what the outlier videos are
actually doing (their titles, their apparent format, their timing). If a
suggested angle would carry meaningful platform-policy risk, say so plainly
rather than omitting the caveat.

Do not invent information not present in the data below. Keep the whole
response under ~900 words.
